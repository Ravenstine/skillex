#!/usr/bin/env node

'use strict';

const pillBox = require('../lib/pill-box')('./pills');
const compile = require('../lib/compile');
const fs      = require('fs');

let schema    = compile(pillBox);

let json      = JSON.stringify(schema, null, 2 ); 

let date      = new Date().getTime().toString();

console.log(JSON.stringify(pillBox.animal, null, 2));

fs.writeFileSync(`build/models/en-US.json`, json, 'utf8');

